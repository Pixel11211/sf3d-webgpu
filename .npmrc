# Avoid npm peer-resolution crashes (e.g. "Exit handler never called!", ERESOLVE)
# with the current dependency graph; this makes a plain `npm install` work everywhere.
legacy-peer-deps=true
fund=false
audit=false
